//这是一个用来给用户自测四则运算的程序
//需要用户自行输入题量，并填写答案，结果会显示正确题数、正确答案


#include<stdio.h>
#include<stdlib.h>
#include<time.h>

//定义一个结构，用于储存一级运算符和二级运算符的判断结果
struct judge
{
	char _symbol;
	int _num;
}symbol[3];
 //当是一级运算符时_num为1，是二级运算符时为2，symbol设置为3是因为考虑到数字最多为4个

//operation函数是用来生成算式的
void operation()
{
	srand((unsigned)time(0));
	int score=0;//统计分数的
	int one,two,three,four,sum_int;//sum_int是整数解
	//double sum_double;      sum_double是有"/"时的解
	char operator_lib[4]={'+','-','*','/'}
	char oper=operator_lib[rand()%4];//这是随机产生运算符
	int number=rand()%3+2;//决定随机产生2~4个数字
	if(number=2)//如果只有两位数
	{
		/*printf("%d %c %d =",one=rand()%10+1,symbol[0]._symbol=oper,two=rand()%10+1);
		scanf("%d",&sum_int);
		if(one symbol[0]._symbol two==sum_int)
			score+=10;*/
	}
	else//两位以上
}


//主函数
int main()
{
	int number;//用户输入题数
	do
	{
		scanf("%d",&number);
		for(i=0;i<number;i++)
			operation();//运算函数
		getchar();//用于获取回车键，如果必要的话
		if(getchar()=='e')//用户输入e可结束测试
			break;
	}while(1);
	return 0;
}
